<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nutricare.model.dao.BoardDao">

	<!-- 게시글 전체 조회 -->
	<select id="selectAll" resultType="Board">
		SELECT * FROM board
	</select>

	<!-- id로 게시글 상세 조회 -->
	<select id="selectById" resultType="Board">
		SELECT * FROM board
		WHERE id =
		#{id}
	</select>

	<!-- 게시글 등록 -->
	<insert id="insert" parameterType="Board" useGeneratedKeys="true" keyProperty="boardId">
		INSERT INTO board (title, content, category)
		VALUES (#{title}, #{content}, #{category})
	</insert>

	<!-- 파일 등록  -->
	<insert id="insertBoardImages" parameterType="Board">
        INSERT INTO board_image (board_id, image_url)
        VALUES
        <foreach collection="images" item="image" separator=",">
            (#{boardId}, #{image.imageUrl})
        </foreach>
    </insert>
    
	<!-- 게시글 내용 수정 -->
	<update id="update" parameterType="Board">
		UPDATE board
		SET title =
		#{title}, content = #{content}
	</update>

	<!-- 게시글 조회수 증가 -->
	<update id="updateViewCnt" parameterType="int">
		UPDATE board
		SET
		view_cnt = view_cnt+1
		WHERE id = #{id}
	</update>
</mapper>